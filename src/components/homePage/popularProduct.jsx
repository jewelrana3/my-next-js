import wait from "@/utilies/wait";


const PopularProduct = async() => {
    await wait()
    return (
        <div>
            <h2>Popular products</h2>
        </div>
    );
};

export default PopularProduct;